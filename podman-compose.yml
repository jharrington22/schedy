version: "3.8"
services:
  db:
    image: docker.io/postgres:16
    environment:
      POSTGRES_USER: resy
      POSTGRES_PASSWORD: resy
      POSTGRES_DB: resy
    ports:
      - "127.0.0.1:5433:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data

  app:
    build:
      context: .
      dockerfile: Containerfile.dev
    env_file:
      - .env
    environment:
      # keep creds in a mounted directory
      HOME: /data/resy
    ports:
      - "8082:8080"
    volumes:
      - ./:/workspace:Z
      - ./.resy:/data/resy:Z
    depends_on:
      - db
    command: ["/bin/sh", "-lc", "$(/usr/local/go/bin/go run . keys) && /usr/local/go/bin/go run . server --migrate=true"]

volumes:
  dbdata: {}
